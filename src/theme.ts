export const theme = {
  // two-tone palette: pure white for primary content, one soft gray for
  // everything secondary (borders, hints, metadata). Emphasis comes from
  // bold/plain combinations, not extra shades.
  primary: '#ffffff',
  gray: '#a1a1aa',
  // the one exception: label on top of the filled yoink button, where
  // white-on-white can't work
  dark: '#18181b',
}
