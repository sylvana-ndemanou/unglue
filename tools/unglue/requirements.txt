# Pulls in torch/torchaudio as dependencies — first install is a large download (1-2GB+).
demucs>=4.0.1
# demucs uses numpy internally (demucs/transformer.py) but doesn't declare it as a dependency.
numpy
